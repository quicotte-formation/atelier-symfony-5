{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <h1>Produit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ produit.titre }}</td>
            </tr>
            <tr>
                <th>AnneeSortie</th>
                <td>{{ produit.anneeSortie }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_produit_index') }}">back to list</a>

    <a href="{{ path('admin_produit_edit', {'id': produit.id}) }}">edit</a>

    {{ include('admin_produit/_delete_form.html.twig') }}
{% endblock %}
